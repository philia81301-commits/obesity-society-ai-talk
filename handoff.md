# 交接檔（handoff.md）

> 任何 Agent、任何電腦接手前**必讀**；收工時**必更新**。
> 本檔只放交接必需的精簡資訊，完整脈絡與決策原因放 Obsidian `obesity-society-ai-talk/專案工作流程.md`。

## ⏯️ 目前做到哪

**2026-08-17（醫院電腦 X108521）**：階段七再推進三項，**全篇由 50 頁變 51 頁**。

- **P9 補強**——依使用者提供的 ADA 2026 Section 8 **官方期刊 PDF** 逐字掃描，
  補入 **§8.8a（A 級，面對面高強度）vs §8.8b（E 級，遠距／app）** 的落差。
  論點從「台灣的個案」升級為**兩套指引各掉一次**：ADA 從 A 掉到 E、台灣從 1A 掉到 2C。
- **新增 P27「門診實務：三個工具串成一次看診」**——`.flow` 流程圖，
  三張去識別化診間實拍**直接放進步驟卡**（輸入 → 調出成效 → 一起決定下一步）。
- **全面重新編號**——舊 P28–P50 → **P29–P51**，`spec.yaml` 與 6 份文件的頁碼引用同步位移。
- **Firebase 已啟用**（設定值已填，`99e2a7b`）——這項在上一版 handoff 被誤記為「未啟用」。

## 🚦 目前狀態

- **可運行**。51 頁離線可播，載入階段零外部請求，Console 零錯誤。
  實測佔高：**P9 0.876／P27 0.887**，皆在 90% 上限內。
- **現場互動已啟用**：P2 文字雲、**P39 投票**（4 選項）。頁碼位移後實測顯示「39 / 51」；
  互動程式綁的是 element ID 不是頁碼，未受影響。
- **PPTX 尚未重跑**（上次是 50 頁版）。內容已異動，交件前必須重跑。
- 內容待補只剩 **P23 敏感議題判定範圍**。

## ➡️ 下一步

1. 🏥 **P23 敏感議題判定範圍**——出處是 `減重門診AI管理.pptx` 第 9 張的投影片備註。
   ⚠️ **2026-08-17 在醫院電腦 X108521 上仍未找到該檔**（原本以為「到醫院就能拿到」，
   結論是**這條線索已斷**）。需使用者自行定位或直接口述內容。
2. **P3「今日架構」的分鐘數要不要調**——Ⅱ 現況實務標示 17 分，
   該段已從 21 頁變 22 頁（P18–P39）。總長沒變，排練後覺得趕再改。
3. **重跑 PPTX 交件**：先在 repo 根目錄開 `python -m http.server 8765`，
   再 `cd slides && python build_pptx.py <輸出.pptx>`。

## ⚠️ 注意事項

### 本次新增

- **🏥 醫院電腦 X108521 的三個限制**（回家不會遇到，但在醫院要知道）：
  1. **網路擋外連下載**——`build_fonts.py` 會在抓 Google Fonts 時失敗（WinError 10061）。
     `pip install` 可用（本次裝了 `pillow-heif`、`fonttools`、`brotli`）。
  2. **不要用 `convert`**——這台的 `convert` 是 **Windows 內建磁碟格式轉換工具**，不是 ImageMagick。
  3. **Obsidian vault 是 `office` 帳號的 OneDrive**，沒有本專案資料夾，**L3 筆記在這台寫不了**。
- **改內文不一定要重跑 `build_fonts.py`**——可用 fontTools 直接比對現有子集：
  讀 `fonts/*-subset.woff2` 的 cmap，和 index.html 的文字集取差集。
  本次新增 P27 後全篇 915 字，**唯一缺字是全形空格 U+3000（不可見、本來就缺）**，故未重跑。
  無法連網時特別有用。
- **`.key-box` 沒有固定高度**——舊 handoff 寫「固定 112px 與字數無關」，
  查 CSS 只有 padding，高度由內容撐。那條在 P9 不適用，**不要照著推論，直接量**。
- **Reveal 一次只排版目前頁 ±2**——沒顯示的 section `scrollHeight` 是 0。
  用 JS 掃全篇佔高會得到一堆假的 0，**必須 `Reveal.slide(i)` 切過去再量**。
- **右欄式版面極度吃行數**——bullet 每多換一行就多 23px。
  P9 一度因此從 0.868 衝到 1.004（爆版 75px），解法是壓 bullet 行數＋併 cite 行數。
- **照片 `object-fit:cover` 會裁成 2.83:1**——4:3 原圖只留中間約四分之一。
  換照片前先模擬裁切，確認螢幕內容沒被切掉。

### 既有

- **P9 的事實修正（最重要）**：`成人肥胖指引 2025〈科技與虛擬醫療在體重管理的運用〉`
  **由講者本人執筆**，含 6 條 GRADE 建議，AI 聊天機器人與 CDSS 各一條（皆 2C）。
  **日後任何「AI 未被指引收錄」的表述都要先核對這份指引。**
- **改 `slides/index.html` 內文後原則上要重跑 `build_fonts.py`**（例外見上方缺字比對法）。
  ⚠️ **emoji 不在 Noto Sans TC 裡**，要標重點請用文字或既有的 `.caveat` 元件。
- **版面 90% 佔高上限**：改完頁面要實測 `section.scrollHeight / 720`。
- **離線原則**：`slides/index.html` 內不得出現任何 CDN 連結。
- **圖檔命名**：新圖不可叫 `cover_*.png`（`.gitignore` 擋掉該樣式）。
- repo 為公開：門診數據一律去識別化、聚合呈現；原始資料檔不進 git。
  **門診實拍的去識別化紀錄見 `reference/clinic-photo-redaction-20260817.md`，換照片前必讀。**
- 引用外部指引時**改寫為摘述、不整段逐字轉錄**（講者自己的著作不在此限）。
- **投影片張數可彈性調整**（2026-08-17 使用者確認），40 分鐘才是硬限制。
- **P25 性別編碼交叉驗證經使用者決定略過**（2026-08-15，記錄用，非遺漏）。

## 🕐 最後更新

- 時間：2026-08-17
- 更新者：Claude Code（Opus 5）@ X108521（醫院）
- 階段：階段七進行中，剩 P23 一項待使用者提供內容
- Git push：✅ 已推（`9ef23a0` 內容變更、`459db86` 收工簿記）
- L3 Obsidian：⚠️ **本次未更新**（這台電腦的 vault 無本專案資料夾），回家用電腦補
- 前一筆：2026-08-16 @ DESKTOP-LVSV9Q5 · ✅ 已推（`0c8cc2a`）
