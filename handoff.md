# 交接檔（handoff.md）

> 任何 Agent、任何電腦接手前**必讀**；收工時**必更新**。本檔只放交接必需的精簡資訊，詳細脈絡放 Obsidian（若有 L3）。

## ⏯️ 目前做到哪
1. **P32-34 案例素材已在家中電腦（`DESKTOP-LVSV9Q5`）定位完成**：減重門診成效分析 skill 產出（月度報告三格式）＋衍生遊戲「減重大冒險」＋減重評估表單（病人自填表單＋醫師用「減重門診輸入工具」`weight-clinic` repo，會產生病歷貼稿）。細節見 `reference/material-inventory.md`。
2. **大綱重排為 v2.1**（`content/outline-50slides.md`）：Ⅱ現況實務改為「減重生態系為主線（評估表單→AI管理系統RAG→月度分析自動化＋遊戲）、骨鬆／肌少症／HP-clinic／pneumonia-clinic為輔線」；Ⅲ落地挑戰段加上成大李南逸主任講義的引用標註（P39、P47）。
3. **`slides/spec.yaml` 已完成**：html-slide-builder skill 格式，50張全部寫好標題／摘要／功能標記，`validate_spec.py` 驗證通過。設計系統覆寫成學術風（白底墨綠，非skill預設的深色霓虹），BG擴大到15頁（每板塊開篇＋Ⅱ段每子案例開篇）、ICON擴大到5頁，皆為使用者明確要求。
4. **卡關**：BG底圖golden sample（P1封面）試了4版，使用者都不滿意，最後一句是「我完全不滿意 收工」——**沒有定案的golden sample，Phase 3-2/4之後的圖都還沒生**。

## 🚦 目前狀態
- 內容面（spec.yaml的文字內容）已經完整且過使用者多輪確認，**可直接沿用**。`rdq/` 底下的規格卡是 revisions:4，執行時以它為準，不要重新訪談。
- 視覺面（AI生成底圖風格）完全卡住，4次嘗試軌跡：①折線圖+分子網絡（嫌太像賣弄數據、偷工減料感）②純水墨筆觸滿版對角線（嫌會擋到圖文、米白太單調）③右下角純白+體重計線稿（嫌太窄化成「只有體重」，肥胖醫學會目標是器官保護與生活品質）④右下角漸層+手捧愛心線稿（使用者未置評細節，直接說「完全不滿意」）
- 4張草稿都在本機 `slides/images/`，**未commit**（都不滿意，不進版本歷史）

## ➡️ 下一步
1. **重新討論BG底圖方向前，建議先問使用者：要不要乾脆不用AI生圖底圖？** 4次都不滿意，且原因一次比一次抽象（風格→構圖→意涵→無理由），可能訊號是「這件事本身優先度不高」或「AI生圖不是對的工具」。改用純CSS極簡設計（一條墨綠細線、一個色塊、或乾脆維持全白不加裝飾）可能比繼續試色圖更有效率。
2. 若使用者仍想要AI底圖：下次開工先問清楚**具體想要什麼意象**（不要再由Agent單方猜測4選1），例如直接問「風格參考圖／競品簡報／某個你喜歡的視覺」。
3. BG方向定案後，才能進入Phase 3-2（其餘14張底圖＋5張ICON圖標總表，帶golden sample平行生成）→ Phase 5（VIZ滑桿）→ Phase 2（HTML骨架，可能要提前做，讓使用者在真實版面中看底圖效果，而非孤立看一張圖——這可能是4次都不滿意的部分原因：脫離版面脈絡很難判斷「會不會擋字」「單不單調」）
4. `slides/spec.yaml` 內容不用重做，直接沿用

## ⚠️ 注意事項
- **不要重新詢問已經確認的內容決策**：50張逐頁標題／摘要、板塊配置、減重生態系為主線的排序、引用標註位置，這些都已經過使用者多輪確認，只有「BG底圖具體長相」卡住
- **本專案可能同時有多台電腦在跑**（本次收工時發現遠端多了一個 `X108521` 幾乎同時推上來的收工commit）：開工前務必先 `git fetch`／`git pull`，動作前確認沒有別台電腦的未讀進度，避免衝突
- `weight-clinic`（醫師輸入工具，https://philia81301-commits.github.io/weight-clinic/）與 `weight-clinic-reports`（月報發布站）是兩個獨立公開repo，簡報若展示畫面截圖記得用空白/測試輸入，不要截到真實病人資料
- repo為**公開**：任何門診原始數據、病人可識別資訊絕不進git；`.xlsx`／`.csv`已在`.gitignore`擋掉，但貼進`.md`／程式碼裡的數字也要注意
- 若簡報中會秀門診數據，動工前務必套用全域臨床資料鐵律（性別編碼0=女/1=男交叉驗證、n<10不當亮點、藥物欄位先掃distinct值、個案限近一年仍回診者）
- 使用者已決定**分段進行**，不要一次做到底，每個階段做完先給使用者看
- PPTX／Word最終成品放 `D:\潘湘如\演講\2026演講\`，不要commit進本repo

## 🕐 最後更新
- 時間：2026-08-14 17:30
- 更新者：Claude Code @ DESKTOP-LVSV9Q5
- Git push：待推（本次收工流程中執行）
