# 交接檔（handoff.md）

> 任何 Agent、任何電腦接手前**必讀**；收工時**必更新**。本檔只放交接必需的精簡資訊，詳細脈絡放 Obsidian（L3）。

## ⏯️ 目前做到哪

**階段四內容補件推進一大段：封面定案 ＋ 四張工具截圖入版 ＋ P9 換上有出處的材料。**

1. **P1 封面定案**（歷經四輪）：抽象化的心／腎／肝色塊置於人形軀幹中央，左側為脂肪細胞發炎訊號網
   （fat cell inflammation），呼應 CKM 概念。標題加擠出式立體效果：字重 900、字級 2.25em、
   左上暖橘 `#BE6C33` 鑲邊（與底圖器官同色）、右下五層漸深墨綠做厚度。
2. **P9 政策頁改寫**：原本三個查無出處的技術採用率（58.17%／36.60%／7.19%）**已移除**，
   改為「國際指引收錄現況」三個可驗證的「0」，並附 DOI 與檢索日。依據見
   `reference/guideline-ai-scan-20260815.md`（本次以兩隻 subagent 查證產出）。
3. **四張工具截圖已嵌入**：P20 醫師輸入工具（測試資料）、P25 月報圖表、P27 遊戲推薦結果、
   P30 骨鬆判定結果（示範個案 65 歲女性／T-score −2.3）。P20／P25 整幅橫式，
   P27／P30 用新增的 `.case-split` 圖文並排。
4. **P4 卡片放大**：新增 `.adv-lg` 修飾類（只套 P4，不動 `.adv-card` 本體）。
5. 字型子集化已重跑（5601 字，**未發現缺字**）；全 50 頁版面重測，**無一超過 90%**。

## 🚦 目前狀態

- **簡報可直接播放**，離線零 CDN 的架構未變動。
- 內容待補從 11 處降到 **1 處**：只剩 **P23 敏感議題判定範圍**（卡在醫院電腦的檔案，見下一步第 1 項）。
  ⚠️ 舊版交接檔把待補框標成 P21／P26／P45，與實際頁碼有落差；正確頁碼是 **P23／P28／P37**。
- 互動元件（P2 文字雲、P38 投票）**程式已完成**，Firestore 設定值未填，
  目前顯示「互動未啟用——請改用口頭舉手」。填 `slides/live-config.js` 即生效。
- **階段五 PPTX 已產出 v1**（2026-08-15）：50 頁、16:9、25 MB，每頁皆有備忘稿。
  位置：`C:\Users\phili\OneDrive\文件\演講\2026演講\肥胖醫學會南區研討會\`
  （**這台沒有 D 槽**，與 agents.md 原記的 `D:\潘湘如\...` 不同，見 agents.md 註記）。
  轉檔腳本 `slides/build_pptx.py` 已進 repo，可重跑。

## ➡️ 下一步

1. 🏥 **【醫院電腦 X108521 才能做】P23 敏感議題判定範圍**——全簡報**最後一個待補框**。
   出處是 `減重門診AI管理.pptx` 第 9 張的投影片備註（完整說明如何讓聊天機器人不產生幻覺、
   敏感問題自動轉介）。**該檔在家用電腦與 Google Drive 都找不到**，素材盤點只記了
   `AI\2026\` 沒有根目錄，2026-08-15 已確認不在這台。到醫院後把檔案推上來或直接口述即可回填。
2. **建 Firebase 專案並填 `slides/live-config.js`**——P2 文字雲／P38 投票的程式碼已全部寫好，
   只差六個設定值。步驟見 `reference/firebase-setup.md`。未填不會壞，會顯示「互動未啟用」。
3. **內容改動後要重跑 PPTX**：`cd slides && python build_pptx.py <輸出.pptx>`
   （需先在 repo 根目錄開 `python -m http.server 8765`）。
   ⚠️ 目前的 v1 是在 **Firebase 未設定、P23 未補**的狀態下產出的，
   所以 P2／P38 印著「互動未啟用——請改用口頭舉手」、P23 還有待補框。
   **這兩項補完後務必重跑一次再交給大會。**
4. ~~P25 性別編碼交叉驗證~~ **使用者於 2026-08-15 決定略過**（記錄用，非遺漏）。

### ✅ 2026-08-15 已完成（原下一步的第 1、3 項）
- P30 指引標註已回填（TOA 2025／AACE 2020／Endocrine Society 2020），
  `osteoporosis-clinic` 的 `fracture-risk.html` 也同步指名版本並補上三個漏判的極高風險條件（v1.1）。
  溯源報告見 `reference/osteoporosis-guideline-scan-20260815.md`，**其餘五項已交班到該 repo 的 handoff**。
- P2／P38 決定接 Firebase，已實作完成並開啟 GitHub Pages（觀眾填答頁在 repo 根目錄 `index.html`）。
- P9 加入 TOA 2025 的 AI 條文對照、P10 補上 Noom 68 週 RCT 數據與四項引用限制、
  P28 設計動機、P31 應用場景、P50 工具連結。P37 選配項目經確認不補。

## ⚠️ 注意事項

- **改動 index.html 內文後，必須重跑字型子集化**：`cd slides && python build_fonts.py`。
- **不得把 index.html 改回 CDN**（演講廳網路不可靠；Google Fonts 的 `chinese-traditional` 子集缺全形標點與羅馬數字）。
- **新增圖檔不可命名 `cover_*.png`**——`.gitignore` 擋掉該樣式（當初為排除四張廢稿），檔案會被忽略、換電腦就不見。
  已定案的封面是 `images/p1_cover_bg_20260815.png`。`images/generated/` 是 draw 技能原始輸出，不進 git。
- **換封面底圖時三個顏色要同步改**：`data-background-color`、`.cover-bg` 遮罩的 hex 與兩個 rgba。
  每次 AI 生圖的紙張底色都不同，不同步左側會出現色差接縫。
- **`.cover-bg` 遮罩不可超過 58%**：脂肪細胞落在畫面 55% 處，遮罩太寬會把它洗成淡影——
  那正是要傳達的 fat cell inflammation。CSS 已寫註解。
- **截圖 `weight-clinic` 工具時只能呼叫 `generateNote()`**：「複製並送出表單」（`copyAndSubmit()`）
  會真的新增一筆到 Google 表單回覆試算表。
- **`.adv-card` 有 7 處在用**，要放大字級請加修飾類，直接改本體會撐爆 P41 的五欄版面。
- **P20／P25 佔高已達 86%／85%**，截圖文字投影時偏小。若要台下讀得到細節需重新裁切
  （P20 只留基本資料＋身體測量；P25 只取兩張圖表），無法靠放大解決。
- **P5 橫條圖長度只代表排序 1-5，不是重要度數值**；該頁 `.cite` 的說明不可移除。
- 掛強烈底圖的 P19／P21／P24 內容需維持 `class="has-bg"`，移除會導致文字壓在插畫上。
- Reveal **會自動把 section 的 class 複製到 `.slide-background`**，不需另寫 JS。
- 本專案曾發生多電腦並行（遠端出現過 `X108521` 的收工 commit）：開工前務必 `git fetch`／`git pull`。
- repo 為**公開**：門診原始數據、病人可識別資訊絕不進 git；工具畫面一律用測試輸入。
- 引用 ADA 素材前先讀 `reference/guideline-ai-scan-20260815.md` 的「三個地雷」
  （§12.6 不是 2026 新增、§5.6 語氣升級未經官方確認、Nature Health 污名研究不含肥胖）。
- 使用者採**分段確認**工作方式，每階段做完先給看，不要一次做到底。

## 🕐 最後更新
- 時間：2026-08-15 07:45
- 更新者：Claude Code @ DESKTOP-LVSV9Q5
- Git push：✅ 已推（`d920bdc`，12 檔／+249 −68，含 5 張新圖約 3.5MB）
