# 交接檔（handoff.md）

> 任何 Agent、任何電腦接手前**必讀**；收工時**必更新**。
> 本檔只放交接必需的精簡資訊，完整脈絡與決策原因放 Obsidian `obesity-society-ai-talk/專案工作流程.md`。

## ⏯️ 目前做到哪

**2026-08-15（第二輪）**：階段四內容補件收尾＋階段五 PPTX 產出，**六個階段全部走完**。

- **P2／P38 現場互動接上 Firebase**：程式全部寫好並開啟 GitHub Pages；
  觀眾填答頁在 repo 根目錄 `index.html`，QR 已產出。**只差建 Firebase 專案填設定值。**
- **P30 指引標註回填**：TOA 2025（114 年 10 月版）為主、AACE/ACE 2020 為極高風險條件出處，
  並註明 FRAX 20%／3% 是美國特定門檻。連帶把 `osteoporosis-clinic` 的
  `fracture-risk.html` 也修了（補三個漏判的極高風險條件，v1.1 已上線）。
- **P9／P10／P28／P31／P50 補完**，P37 選配項目確認不補。內容待補從 11 處降到 **1 處**。
- **階段五 PPTX v1 產出**：50 頁、16:9、25 MB，每頁附備忘稿。

## 🚦 目前狀態

- **可運行**。HTML 簡報 50 頁離線可播，載入階段零外部請求。工作副本乾淨、與 GitHub 同步。
- **內容待補只剩 1 處：P23 敏感議題判定範圍**（需醫院電腦的檔案，見下一步）。
  ⚠️ 舊版交接檔把待補框標成 P21／P26／P45，**與實際頁碼有落差**；正確是 **P23／P28／P37**。
- **Firebase 互動程式已完成但未啟用**：設定值未填，P2／P38 目前顯示
  「互動未啟用——請改用口頭舉手」。填 `slides/live-config.js` 即生效，不填也不會壞。
- **PPTX v1 不能直接交件**：它是在 Firebase 未設定、P23 未補的狀態下產出的。
- 骨鬆工具溯源報告列出的其餘五項，已交班到 `osteoporosis-clinic` 的 handoff（不在本專案處理）。

## ➡️ 下一步

### 上台前定稿（階段七，2026-08-15 使用者交辦）

1. **PPTX 要有右下角頁碼**。目前 `slides/build_pptx.py` 的 `add_style_tag` 為了清掉操作介面，
   把 `.slide-number` 一起隱藏了。建議改成只隱藏 `.controls` 與 `.progress`、**保留頁碼**
   （與 HTML 版一致），而不是在 python-pptx 端另外加文字方塊。
2. **P9 政策段補新資料**——使用者手上有新資料要補充，待提供。
   P9 現況：左欄 2025 衛福政策白皮書、右欄國際指引收錄現況（ADA 三項皆 0 次），
   收在 `.key-box` 並附 TOA 2025 p.18 的 AI 條文對照。
3. **補門診實地照片**：門診使用工具的現場照片。
   ⚠️ **去識別化**：不可入鏡病人、健保卡、螢幕上的個資或可辨識的病歷畫面；repo 是公開的。
4. **P22 改為整頁資訊呈現**。P22 是主線案例 02（AI 管理系統）2/3「RAG 架構如何降低幻覺風險」，
   目前用 clip-path 滑桿揭露做有／無 RAG 的對比，**使用者回饋效果不好**。
   使用者指示方向原話：**「整頁資訊」**——理解為改成靜態整頁把資訊攤開、不再用滑桿互動，
   **但具體形式（左右並列？表格？流程圖？）動工前先跟使用者確認。**

### 需要特定條件才能做

5. 🏥 **【醫院電腦 X108521 才能做】P23 敏感議題判定範圍**——全簡報最後一個待補框。
   出處是 `減重門診AI管理.pptx` 第 9 張的投影片備註。**該檔在家用電腦與 Google Drive 都找不到**，
   素材盤點只記了 `AI\2026\` 沒有根目錄，2026-08-15 已確認不在這台。
   到醫院後把檔案推上來或直接口述即可回填。
6. **建 Firebase 專案並填 `slides/live-config.js`**（只有使用者能做，需登入 Console）。
   步驟、Firestore 安全性規則全文、當天檢查清單都在 `reference/firebase-setup.md`。

### 最後一定要做

7. **上述補完後重跑 PPTX 再交件**：
   先在 repo 根目錄開 `python -m http.server 8765`，再 `cd slides && python build_pptx.py <輸出.pptx>`。

## ⚠️ 注意事項

- **改 `index.html` 內文後必須重跑 `cd slides && python build_fonts.py`**，否則新字變豆腐。
  ⚠️ **emoji 不在 Noto Sans TC 裡**（⚠️ 已踩過），要標示重點請用文字或既有的 `.caveat` 元件。
  另外該腳本遇到缺字時會因 cp950 主控台編碼 crash 在報告那行，**不影響子集產出**，不用緊張。
- **版面 90% 佔高上限**：改完頁面要用瀏覽器實測 `section.scrollHeight / 720`。
  空間不夠時的既有手段：對 `.two-col` 套 `.tight` 修飾類（已用於 P2／P9／P10）。
- **離線原則**：`slides/index.html` 內不得出現任何 CDN 連結。Firebase SDK 已本地化到 `vendor/`，
  且**必須用 compat(UMD) 版**——v11 的 ESM 在 `file://` 會被 CORS 擋掉，而簡報是本機檔案播放。
- **圖檔命名**：新圖不可叫 `cover_*.png`（`.gitignore` 擋掉該樣式）。
- **成品位置因電腦而異**：家用 DESKTOP-LVSV9Q5 **沒有 D 槽**，
  PPTX 存 `C:\Users\phili\OneDrive\文件\演講\2026演講\肥胖醫學會南區研討會\`；
  醫院端的 `D:\潘湘如\演講\2026演講\` 應為同一份雲端資料夾的不同掛載點，**到醫院時請確認**。
- **P25 性別編碼交叉驗證經使用者決定略過**（2026-08-15，記錄用，非遺漏）。
- repo 為公開：門診數據一律去識別化、聚合呈現；原始資料檔不進 git。
- 引用外部指引時**改寫為摘述、不整段逐字轉錄**（`osteoporosis-guideline-scan` 已依此調整）。

## 🕐 最後更新

- 時間：2026-08-15（第二輪收工）
- 更新者：Claude Code（Opus 5）@ DESKTOP-LVSV9Q5（家）
- 階段：六個階段全部完成，進入階段七「上台前定稿」
- Git push：✅ 已推（本輪共 6 個 commit：`22dc8fe` Firebase 互動、`0f42770` P30／P9、
  `59abbfd` P10、`827d36a` P28／P31／P50、`27f4a9f` 階段五 PPTX、`cca183b` 收工文件）
  另：`osteoporosis-clinic` 也推了 2 個（`5811891` 工具 v1.1、`9a975e2` 交班）
- 前一筆：2026-08-15 07:45 @ DESKTOP-LVSV9Q5 · ✅ 已推（`d920bdc`、`ff75100`）
