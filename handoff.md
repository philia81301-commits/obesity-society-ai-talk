# 交接檔（handoff.md）

> 任何 Agent、任何電腦接手前**必讀**；收工時**必更新**。本檔只放交接必需的精簡資訊，詳細脈絡放 Obsidian（若有 L3）。

## ⏯️ 目前做到哪

**階段四（HTML 簡報）主體完成，簡報已可直接上台播放。**

1. **50 頁骨架全數產出**（`slides/index.html`），版型齊備：板塊羅馬數字、標題細線、頁尾、案例頁左側編號色塊、大數字卡、RAG 對比滑桿。
2. **內容補實**：以使用者提供的 2025-05-25 舊簡報《The Role of Digital Health in Obesity Management》PDF，回填 P2、P5、P6、P7、P8、P9、P15 共 7 頁的文獻與數字（含完整期刊出處）。P21／P23 補上 RAG 知識庫組成（**衛教單張＋治療指引**，經使用者確認）。P34 依使用者提供的連結，從「社區篩檢實測數據」（無數據）改為**肌少症衛教小測驗**。
3. **視覺系統定案**（歷經三輪使用者回饋：字看不清 → 配色單調 → 壅擠沒特色）：
   - 板塊識別色：Ⅰ墨綠／Ⅱ深青藍／Ⅲ赭棕／結語橄欖，各配一個對比色 `--pop`
   - 17 個內嵌 SVG 線性圖標（**取代 AI 生圖**），25+ 處引用
   - 深色反白結論塊 `.key-box` 17 處
   - 資料視覺化：P3、P5、P6、P7、P8、P12、P14、P16、P23、P24、P34、P42-44、P48
4. **底圖定案**：P19／P21／P24 三張手繪粗筆觸插畫（gpt-image-2，共 NT$0.9），深青藍＋暖橘呼應 Ⅱ 板塊色；對應內容頁用同款極淡版（opacity 0.08-0.1）延續風格。
5. **離線化完成並實測**：零 CDN 連結，reveal.js／字型／底圖全部本地，實測載入時外部請求 0 筆。

## 🚦 目前狀態

- **簡報可直接播放**。50 頁全部通過版面檢查：無一頁溢出，最高佔可用高度 89%（守則：超過 90% 即視為壅擠）。
- 內容面尚有 **11 處待補**，全部已在頁面上以黃色虛線框標示，主要是**截圖類**。
- 互動元件（P2 文字雲、P38 投票）仍是靜態佔位，**Firebase 尚未接**。
- PPTX 版本（階段五）尚未開始。

## ➡️ 下一步

1. **補截圖素材**（使用者提供）：P20 醫師輸入工具畫面（**務必用測試資料，不可截到真實病人**）、P25 月報畫面、P27 減重大冒險畫面、P30 骨鬆決策流程圖與指引版本年份。
2. **補數據**：P9 三個技術採用率百分比（58.17%／36.60%／7.19%）目前查無出處——來自舊簡報第 18 頁但該頁未標來源，學術場合引用前需補，否則改為文字敘述。P23 敏感議題判定範圍與成效觀察。
3. 決定 **P2／P38 互動元件**是否真的要接 Firebase（現場需網路，與離線播放原則衝突，可能改為口頭舉手）。
4. 階段五：PPTX 版本（存 `D:\潘湘如\演講\2026演講\`，不進 repo）。

## ⚠️ 注意事項

- **改動 index.html 內文後，必須重跑字型子集化**，否則新字變豆腐：`cd slides && python build_fonts.py`（腳本會自動偵測缺字並警告）。詳見 agents.md「離線化」章節。
- **不得把 index.html 改回 CDN**。演講廳網路不可靠；且 Google Fonts 的 `chinese-traditional` 子集**缺全形標點（，／：）與羅馬數字（Ⅰ Ⅱ Ⅲ）**，用了會滿頁豆腐字。
- **P5 橫條圖長度只代表排序 1-5，不是重要度數值**（原研究未提供效果量）。該頁 `.cite` 內的這句說明**不可移除**，否則在學術場合會被當成效果量誤讀。
- 掛強烈底圖的頁面（P19／P21／P24）內容需維持 `class="has-bg"`：靠 CSS 漸層遮罩讓左側有乾淨底色，**移除會導致文字壓在插畫上**。
- Reveal **會自動把 section 的 class 複製到 `.slide-background`**，不需另寫 JS 標記（本次繞過一次遠路）。
- 本專案曾發生多電腦並行（遠端出現過 `X108521` 的收工 commit）：開工前務必 `git fetch`／`git pull`。
- repo 為**公開**：門診原始數據、病人可識別資訊絕不進 git；簡報若秀工具畫面一律用空白／測試輸入。
- 若簡報要放門診數據，動工前套用全域臨床資料鐵律（性別編碼 0=女／1=男交叉驗證、n<10 不當亮點、藥物欄位先掃 distinct 值、個案限近一年仍回診者）。
- 使用者採**分段確認**工作方式，每階段做完先給看，不要一次做到底。
- 早期 4 張未過關的封面草稿（`slides/images/cover_*.png`）已加入 `.gitignore`，檔案保留本機供對照。

## 🕐 最後更新
- 時間：2026-08-14 23:55
- 更新者：Claude Code @ DESKTOP-LVSV9Q5
- Git push：✅ 已推（`97e0e8e`，13 檔／10.8MB）
