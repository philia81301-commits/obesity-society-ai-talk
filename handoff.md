# 交接檔（handoff.md）

> 任何 Agent、任何電腦接手前**必讀**；收工時**必更新**。
> 本檔只放交接必需的精簡資訊，完整脈絡與決策原因放 Obsidian `obesity-society-ai-talk/專案工作流程.md`。

## ⏯️ 目前做到哪

**2026-08-16**：階段七「上台前定稿」推進四項，**Agent 能做的部分已全部做完**。

- **P9 政策段改寫**——原斷言「AI 未寫進任何肥胖治療建議條文」**經查為錯**（見下方 ⚠️），
  改為**證據等級落差**論述：ADA 2026 三份文件 AI 提及 0 次；台灣 2025 醫療科技介入 1A，
  但 AI 聊天機器人與 CDSS 僅 **2C**。並補入 2026-05 衛福部生成式 AI 指引。
- **P22 重做**——移除 clip-path 滑桿（使用者回饋：RAG 內容太薄，拉不出揭露感），
  改為整頁機制對照「答得像 vs 答得準」。知識庫確認為**學會指引＋教科書**，P23 已同步對齊。
- **P40 擴充**——第三段原本九頁全講個資，補上另一半：AI 非法律主體、LINE 上的 AI 回覆
  可能構成醫療廣告。條號經 subagent 逐條查證。
- **P49 加金句**——「AI 提供地圖，醫師擔任嚮導」（原句嚮導是營養師，因一人門診無營養師而改）。
- **PPTX 右下角頁碼**——`build_pptx.py` 不再隱藏 `.slide-number`，另修掉超連結底線、封面明確不顯示。

## 🚦 目前狀態

- **可運行**。50 頁離線可播，載入階段零外部請求。實測佔高：P9 0.868／P22 0.779／P23 0.718／
  P40 0.758／P49 0.779，全部在 90% 上限內。Console 零錯誤，字型未發現缺字。
- **PPTX 轉檔已驗證**（50 頁 26.1 MB），但**測試檔輸出到暫存區，OneDrive 上的 v1 未動**——
  因為門診照片與 P23 都還沒補，現在不是交件時機。
- **Firebase 互動程式已完成但未啟用**：設定值未填，P2／P38 顯示「互動未啟用」。
  填 `slides/live-config.js` 即生效，不填也不會壞。**2026-08-16 已決定建新專案**（不沿用共用專案）。
- 內容待補只剩 **P23 敏感議題判定範圍**（需醫院電腦）。

## ➡️ 下一步

**剩下三項全部只有使用者能推進，Agent 這邊沒有可做的了。**

1. **建 Firebase 專案並填 `slides/live-config.js`**（需登入 Console）。
   步驟、Firestore 安全性規則全文、當天檢查清單都在 `reference/firebase-setup.md`。
   ⚠️ 別用 `html-slide-builder` skill 的共用專案寫法，理由見注意事項。
2. **補門診實地照片**。⚠️ **去識別化**：不可入鏡病人、健保卡、螢幕上的個資或可辨識的病歷畫面；repo 公開。
3. 🏥 **【醫院電腦 X108521 才能做】P23 敏感議題判定範圍**——出處是 `減重門診AI管理.pptx`
   第 9 張的投影片備註，該檔在家用電腦與 Google Drive 都找不到。到醫院後推上來或口述即可回填。

### 最後一定要做

4. **上述補完後重跑 PPTX 再交件**：
   先在 repo 根目錄開 `python -m http.server 8765`，再 `cd slides && python build_pptx.py <輸出.pptx>`。

## ⚠️ 注意事項

### 本次新增

- **P9 的事實修正（最重要）**：`成人肥胖指引 2025〈科技與虛擬醫療在體重管理的運用〉`
  **由講者本人執筆**，含 6 條 GRADE 建議，AI 聊天機器人與 CDSS 各一條（皆 2C）。
  **日後任何「AI 未被指引收錄」的表述都要先核對這份指引。**
  來源：`C:\Users\phili\OneDrive\文件\obesity\docx\[終版]_成人肥胖指引2025_...docx`（附 42 篇 DOI 文獻，
  可用來替換北區簡報那批無出處數字）。
- **`.key-box` 高度固定 112px，與字數無關**。P49 一度因此卡在 0.899（零餘裕）。
  **要省高度就換容器**（改併進既有的 `.quote-box` 後降到 0.779），不是改字。
  這是繼「對 `.two-col` 套 `.tight`」之後的第二個可用手段。
- **Reveal 用 inline style 設 `.slide-number` 的 `display:block`**——樣式表規則權重贏不了，
  要覆蓋必須加 `!important`（封面隱藏頁碼那條就是這樣才生效）。
- **Reveal 把頁碼包在 `<a>` 裡，帶超連結底線**。HTML 上不明顯，但 PPTX 是直接截圖，底線會烙進成品。
- **`slides/shots/` 已加入 `.gitignore`**（轉檔每次產生 50 張、實測 27 MB）。
- **`D:\` 是隨身碟，不是固定磁碟**。北區素材原在 `D:\北區\`，已備份三份到
  `C:\Users\phili\OneDrive\文件\演講\2026演講\肥胖醫學會南區研討會\`。
  舊 handoff 寫「家用電腦沒有 D 槽」不精確，是隨身碟插上才有。
- **讀 docx 的工具鏈**：`markitdown` 未安裝 docx 依賴會失敗，改用 `python-docx`；
  另外 `markitdown` 輸出重導到檔案會用 cp950 編碼，中文會壞，要加 `PYTHONIOENCODING=utf-8`。
- **不要採用 `html-slide-builder` skill 的 Firebase 寫法**：它用 gstatic CDN ＋
  `<script type="module">`（ESM），在本專案會**兩頭都掛**——違反離線原則，且 ESM 在 `file://`
  會被 CORS 擋掉。本專案已改用本地 compat(UMD) 版，程式全部寫好，只差設定值。

### 既有

- **改 `slides/index.html` 內文後必須重跑 `cd slides && python build_fonts.py`**，否則新字變豆腐。
  ⚠️ **emoji 不在 Noto Sans TC 裡**，要標重點請用文字或既有的 `.caveat` 元件。
  該腳本遇缺字時會因 cp950 主控台編碼 crash 在報告那行，**不影響子集產出**。
- **版面 90% 佔高上限**：改完頁面要用瀏覽器實測 `section.scrollHeight / 720`。
  ⚠️ **P9 目前 0.868，只剩約 23px**，要再加東西必須先砍別的。
- **離線原則**：`slides/index.html` 內不得出現任何 CDN 連結。
- **圖檔命名**：新圖不可叫 `cover_*.png`（`.gitignore` 擋掉該樣式）。
- repo 為公開：門診數據一律去識別化、聚合呈現；原始資料檔不進 git。
- 引用外部指引時**改寫為摘述、不整段逐字轉錄**（講者自己的著作不在此限）。
- **P25 性別編碼交叉驗證經使用者決定略過**（2026-08-15，記錄用，非遺漏）。

## 🕐 最後更新

- 時間：2026-08-16
- 更新者：Claude Code（Opus 5）@ DESKTOP-LVSV9Q5（家）
- 階段：階段七進行中，Agent 可做的部分已完成，剩三項待使用者推進
- Git push：✅ 已推（`0c8cc2a`）
- 前一筆：2026-08-15（第二輪收工）@ DESKTOP-LVSV9Q5 · ✅ 已推（`cca183b`、`73e594f`）
